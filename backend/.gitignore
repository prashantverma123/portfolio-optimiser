# Ignore Gradle project-specific cache directory
.gradle
.kotlin

# Ignore Gradle build output directory
build

# Ignore the wrapper JAR so the repository stays binary-free
gradle/wrapper/gradle-wrapper.jar
